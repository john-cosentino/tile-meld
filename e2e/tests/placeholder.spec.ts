import { test, expect } from "@playwright/test";

// Placeholder only -- not wired into Phase 0 CI (no app to test against
// yet). The first real spec is the Phase 6 two-context smoke test.
test.skip("placeholder: real specs start in Phase 6", async () => {
  expect(true).toBe(true);
});
